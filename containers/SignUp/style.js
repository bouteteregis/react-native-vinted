import { StyleSheet } from "react-native";

export default StyleSheet.create({
  textStyle: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  otherStyle: {
    position: "absolute",
    justifyContent: "center",
  },
});
